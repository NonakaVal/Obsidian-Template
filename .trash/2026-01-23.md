---
created: 2026-01-23 @ 10:22
tags:
  - calendar/daily
week: '[[2026 Week 04]]'
---

2026-01-23’s Note

[[2026-01-22|↶ Ontem]] | [[2026-01-24|Amanhã ↷]]

# Daily Mood 

 `INPUT[inlineSelect(option('🙂 – Neutral'), option('😄 – Happy'), option('😐 – Meh'), option('😞 – Sad'), option('😠 – Frustrated'), showcase):daily-mood]`


---

# Gratidão

## _Comece o dia escrevendo pelo que você é grato(a) para cultivar um senso de contentamento e apreciação_


Perfeito — vamos **reduzir tudo para Dataview puro**, sem JS, mantendo a **essência informacional** (contagem, links, datas), mas aceitando as **limitações naturais do Dataview**:  
👉 sem lógica condicional complexa,  
👉 sem estilos inline,  
👉 sem cálculos derivados avançados (percentuais, médias customizadas).

Abaixo está uma **versão enxuta, funcional e legível**, focada em **overview + tabela**, usando apenas `dataview`.

---

## 📊 Resumo simples (Dataview)

```dataviewjs
// --------------------
// COLEÇÕES
// --------------------
const colecoes = dv.pages('"X/Collections"')
  .sort(p => p.file.ctime, 'desc')
  .slice(0, 33);

// --------------------
// MÉTRICAS
// --------------------
let totalColecoes = colecoes.length;
let colecoesComNotas = 0;
let totalNotas = 0;
let totalLinks = 0;

for (const c of colecoes) {
  const inlinks = c.file.inlinks?.length ?? 0;
  const outlinks = c.file.outlinks?.length ?? 0;

  if (inlinks > 0) colecoesComNotas++;
  totalNotas += inlinks;
  totalLinks += outlinks;
}

// --------------------
// RESUMO
// --------------------
dv.paragraph(`
📂 **Coleções:** ${totalColecoes}  
📝 **Com notas:** ${colecoesComNotas}  
📑 **Total de notas:** ${totalNotas}  
🔗 **Total de links:** ${totalLinks}
`);

// --------------------
// TABELA
// --------------------
dv.table(
  ["📂 Coleção", "📝 Notas", "🔗 Links", "📅 Criada em"],
  colecoes.map(c => [
    c.file.link,
    c.file.inlinks?.length ?? 0,
    c.file.outlinks?.length ?? 0,
    c.file.ctime?.toFormat("dd/MM/yyyy") ?? "-"
  ])
);

```

> 🔹 Esse bloco funciona como um **resumo agregado** das últimas 33 coleções.

---

## 📋 Tabela principal das coleções

```dataview
TABLE WITHOUT ID
	file.link AS "📂 Coleção",
	file.inlinks AS "📝 Notas",
	length(file.inlinks) AS "🔢 Total"
FROM "X/Collections"
SORT file.ctime DESC
LIMIT 33

```

---

## 🧭 Versão com indicador visual simples (emoji por volume)

Dataview não permite `if` real, mas dá pra **comunicar estado visualmente** com colunas auxiliares:

```dataview
TABLE
	file.link AS "📂 Coleção",
	length(file.inlinks) AS "📝 Notas",
	choice(length(file.inlinks) > 10, "📚",
	choice(length(file.inlinks) > 5, "📖",
	choice(length(file.inlinks) > 0, "📘", "📁"))) AS "Estado",
	file.ctime AS "📅 Criada em"
FROM "X/Collections"
SORT file.ctime DESC
LIMIT 33
```

---

## ⚠️ O que foi removido (e por quê)

|Item|Motivo|
|---|---|
|Cards estilizados|Dataview não suporta CSS/HTML|
|Percentuais e médias|Exigem loop/cálculo JS|
|Cores dinâmicas|Não disponíveis sem JS|
|Cálculo de “dias desde criação”|Dataview não calcula `DateTime.now()`|

---

## 🧠 Como evoluir a partir daqui

Se você quiser avançar **sem voltar ao dataviewjs**, boas próximas ideias são:

- Criar **views separadas** (ex: “Coleções Vazias”, “Coleções Ativas”) usando `WHERE length(file.inlinks) > 0`.
    
- Usar **páginas-resumo fixas** (Index dashboards) com múltiplos blocos Dataview pequenos em vez de um mega painel.
    
- Padronizar **frontmatter** (`status`, `tier`, `progress`) nas coleções — isso desbloqueia filtros e métricas muito mais ricas só com Dataview.
    
- Se quiser, posso te entregar uma **arquitetura híbrida**: Dataview para leitura rápida + um único DataviewJS só para métricas globais, bem limpo e reutilizável.

---

# Registro de Trabalho #inlog



---

# Algo bom

_Recorde e anote acontecimentos positivos do seu dia, por menores que sejam._
   

---





# Capture
- [ ] ✅ task - 11:06 \ [[2026-01-23]]
